# Copy this file to  ~/.config/fish/secrets.fish  and fill in real values.
# secrets.fish is git-ignored so credentials never end up in the repo.

# Password used by the `kali` function in config.fish:
set -gx KALI_SSH_PASS "changeme"
